import React from 'react';
import PropTypes from 'prop-types';
import styles from './HeaderInterfaces.module.css';

const HeaderInterfaces = () => (
  <div className={styles.HeaderInterfaces}>
    HeaderInterfaces Component
  </div>
);

HeaderInterfaces.propTypes = {};

HeaderInterfaces.defaultProps = {};

export default HeaderInterfaces;
